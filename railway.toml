[build]
builder = "nixpacks"
buildCommand = "RUN npm install --legacy-peer-deps && npm run build
"

[deploy]
startCommand = "npm run preview"
healthcheckPath = "/"
healthcheckTimeout = 300

[deploy.envs]
NODE_ENV = "production"
